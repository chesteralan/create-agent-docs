# API Contracts — {{projectName}}
{{> header}}

This document details the communication contracts, response models, and conventions for interacting with the **{{projectName}}** backends.

---

## 🌐 Endpoint Protocols

- **Base URL (Local)**: `http://localhost:{{#eq backend "Firebase"}}5001{{else}}3000{{/eq}}`
- **Format**: All payloads must be structured as `application/json`.
- **Headers**:
  - `Content-Type: application/json`
  {{#ne authProvider "None"}}
  - `Authorization: Bearer <TOKEN>` (For authenticated requests)
  {{/ne}}

---

## 🔒 Error Payload Structure

All API error responses must adhere to the following payload contract:

```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE_STRING",
    "message": "Human readable error explanation.",
    "details": {}
  }
}
```

---

{{#eq backend "Express"}}
### Express REST Endpoint Patterns
- **Route Structure**: Organize routes by domain (`/api/users`, `/api/posts`, `/api/auth`).
- **Middleware**: Apply authentication, validation, and rate-limiting middleware per route group.
- **Error Handling**: Use a centralized Express error-handling middleware to return consistent `{ success, error }` payloads.
- **Validation**: Use Zod or Joi schemas to validate request bodies before they reach route handlers.

{{/eq}}
{{#eq backend "Firebase"}}
### Firebase Integration Patterns
- **Callable Functions**: Use `onCall` for authenticated client-server interactions.
- **Firestore Rules**: Enforce data access control at the document level using security rules.
- **Batch Operations**: Use batched writes for atomic multi-document updates.

{{/eq}}
## 📝 Integration Guidelines

> [!NOTE]
> ### 1. Client Requests
> All client operations should wrap API fetches in try/catch handlers, mapping raw errors to user-friendly messages.
>
> ### 2. Cors Policies
> Allow origins must be restricted to standard local hosts during development, and explicit production domains on deployment.

{{> footer}}
