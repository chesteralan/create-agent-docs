# Phase 3 — Template System Enhancements

## Status: In Progress (~70% done)

8 templates exist but partials, layouts, and syntax validation are missing.

---

## Tasks

### 3.1 Add Handlebars Partials

Register reusable partial snippets so templates stay DRY.

- [ ] Create `src/templates/partials/` directory
- [ ] Extract `header.md.hbs` partial — project name, framework badge, generated date
- [ ] Extract `footer.md.hbs` partial — "Last generated by create-agent-docs" notice
- [ ] Register partials in `template-engine.ts` via `Handlebars.registerPartial()`
- [ ] Update existing templates to use `{{> header}}` and `{{> footer}}`

### 3.2 Add Layout System

- [ ] Create `src/templates/layouts/default.md.hbs` with `{{{body}}}` placeholder
- [ ] In `renderTemplate`, wrap content with layout when rendering
- [ ] Allow templates to opt out of layout via frontmatter

### 3.3 Template Syntax Validation

- [ ] In `template-engine.ts`, wrap `Handlebars.compile()` in a try/catch
- [ ] Log specific syntax errors (unclosed `{{`, unknown helper, etc.)
- [ ] Return a clear error message pointing to the template file and line

### 3.4 Template Variable Audit

- [ ] Document all available template variables in a comment at the top of each template
- [ ] Add `{{generatedDate}}` and `{{cliVersion}}` variables to all templates
- [ ] Ensure `{{frontendFramework}}`, `{{backend}}`, etc. are consistently used across all 8 templates

---

## Verification

- [ ] `yarn test` passes (add tests for partial rendering)
- [ ] Generated docs include header/footer
- [ ] Malformed template produces a clear error
